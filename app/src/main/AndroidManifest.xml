<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NimmaGuru">

        <!-- LAUNCHER -->
        <activity android:name=".activities.SplashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- AUTH -->
        <activity android:name=".activities.LoginActivity"/>
        <activity android:name=".activities.RegisterActivity"/>
        <activity android:name=".activities.OnboardingActivity"/>

        <!-- STUDENT FLOW -->
        <activity android:name=".activities.StudentLoginActivity"/>
        <activity android:name=".activities.StudentProfileActivity"/>
        <activity android:name=".activities.HomeActivity"/>
        <activity android:name=".activities.ProfileActivity"/>
        <activity android:name=".activities.BookingActivity"/>
        <activity android:name=".activities.SearchActivity"/>
        <activity android:name=".activities.CalendarActivity"/>
        <activity android:name=".activities.AppreciationActivity"/>
        <activity android:name=".activities.NotificationsActivity"/>
        <activity android:name=".activities.SettingsActivity"/>
        <activity android:name=".activities.AiAssistantActivity"/>

        <!-- GURU FLOW -->
        <activity android:name=".activities.GuruLoginActivity"/>
        <activity android:name=".activities.GuruProfileSetupActivity"/>
        <activity android:name=".activities.GuruHomeActivity"/>
        <activity android:name=".activities.EditProfileActivity"/>

        <!-- OTHER -->
        <activity android:name=".activities.NearbyGuruActivity"/>

    </application>
</manifest>
